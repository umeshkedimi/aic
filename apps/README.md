# apps

Deployable services live here (FastAPI services, workers, CLIs) — one directory per app, each a
member of the `uv` workspace. Empty as of T1; the first app (`checkout-service`,
`payment-service`) lands in T2.
