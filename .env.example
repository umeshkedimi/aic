# AIC Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# APPLICATION
# =============================================================================
AIC_ENV=development
AIC_DEBUG=true
AIC_LOG_LEVEL=INFO

# =============================================================================
# SERVER
# =============================================================================
AIC_HOST=0.0.0.0
AIC_PORT=8000
AIC_WORKERS=1

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================
AIC_DATABASE_URL=postgresql+asyncpg://aic:aic_dev_password@localhost:5432/aic
AIC_DATABASE_POOL_SIZE=5
AIC_DATABASE_MAX_OVERFLOW=10

# =============================================================================
# CACHE (Redis)
# =============================================================================
AIC_REDIS_URL=redis://localhost:6379/0
AIC_REDIS_MAX_CONNECTIONS=10

# =============================================================================
# VECTOR STORE (Qdrant)
# =============================================================================
AIC_QDRANT_HOST=localhost
AIC_QDRANT_PORT=6333
AIC_QDRANT_COLLECTION=aic_knowledge

# =============================================================================
# LLM CONFIGURATION
# =============================================================================
# Provider: "openai" or "azure"
AIC_LLM_PROVIDER=openai

# OpenAI
OPENAI_API_KEY=sk-your-api-key-here
AIC_OPENAI_MODEL=gpt-4-turbo-preview
AIC_OPENAI_EMBEDDING_MODEL=text-embedding-3-small
AIC_OPENAI_MAX_TOKENS=4096
AIC_OPENAI_TEMPERATURE=0.1

# Azure OpenAI (alternative)
AIC_AZURE_OPENAI_API_KEY=
AIC_AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AIC_AZURE_OPENAI_DEPLOYMENT=gpt-4
AIC_AZURE_OPENAI_API_VERSION=2024-02-15-preview

# =============================================================================
# OBSERVABILITY
# =============================================================================
AIC_OTLP_ENDPOINT=http://localhost:4317
AIC_OTLP_ENABLED=true
AIC_METRICS_ENABLED=true

# =============================================================================
# RAG CONFIGURATION
# =============================================================================
AIC_RAG_CHUNK_SIZE=512
AIC_RAG_CHUNK_OVERLAP=50
AIC_RAG_TOP_K=5

# =============================================================================
# SECURITY
# =============================================================================
AIC_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]
AIC_API_KEY_HEADER=X-API-Key
